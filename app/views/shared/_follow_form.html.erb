<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.follow?(@user) %>
    <%= render 'shared/unfollow' %>
  <% else %>
    <%= render 'shared/follow' %>
  <% end %>
  </div>
<% end %>