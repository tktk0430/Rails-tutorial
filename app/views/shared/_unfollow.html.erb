<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), #params[:id]にはこのrelationshipの番号が入る
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-danger" %>
<% end %>